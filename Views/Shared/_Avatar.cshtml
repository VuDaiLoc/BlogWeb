@model string
<img src="@(
    string.IsNullOrEmpty(Model) || Model == "default.png"
        ? "/images/male.png"
        : $"/uploads/avatar/{Model}"
)" alt="Avatar" class="rounded-circle" width="32" height="32" />
